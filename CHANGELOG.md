# 更新日志

本文件记录 PwdBook 各版本的面向用户变更。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [Unreleased]

## [1.22.0] - 2026-06-22

### 新增

- **条目附件** — 详情页可为条目添加、打开、另存为、删除附件；文件 AES 加密存于 `{userData}/attachments/*.enc`；每条条目最多 **10** 个、单文件最大 **5 MB**；IPC：`attachments:list` / `add` / `delete` / `open` / `save-as`。
- **自定义字段** — 详情页可添加任意名称的 name/value 字段（如 PIN、安全问题答案）；每条条目最多 **20** 个；参与搜索与 CSV 导出「自定义字段」列；同步 bundle 含 `customFields`。
- **列表布局切换** — 条目列表工具栏支持 **列表 / 方块** 两种布局；偏好写入 `localStorage`（`pwdbook-entry-list-layout`）。
- **同步附件** — Wi-Fi 与文件夹同步支持附件元数据与 `.pwdattach` 加密文件；SyncBundle **`version` 升至 2**；删除 tombstone 参与 LWW 合并（`attachmentSyncService.ts`）。

### 改进

- **JSON 导出/导入** — PwdBook JSON 备份含条目附件（Base64 内嵌）；导入保留原条目 ID 以关联附件；`EXPORT_PAYLOAD_VERSION` 升至 **2**。
- **导出明文确认** — **设置 → 数据 → 导出数据** 及 **设置 → 安全 → 恢复密钥** 页快捷导出 JSON/Excel 前，弹出明文安全确认对话框。

### 修复

- **详情页 UI** — 附件与自定义字段「添加」按钮样式统一（`detail-section-add-btn`）；自定义字段区块结构与间距修正。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.22.0（附件、自定义字段、布局切换、SyncBundle v2、JSON 附件备份等）。

## [1.21.0] - 2026-06-16

### 新增

- **开机自动启动** — **设置 → 安全** 首行新增开关（默认关闭）；开启后通过 Electron `app.setLoginItemSettings` 注册系统登录项；启动时与 `settings:update` 时由 `launchAtLogin.ts` 同步 OS 状态（仅正式打包版本生效，开发模式跳过）。

### 改进

- **恢复访问菜单** — 移除锁定页「我有 JSON 备份」选项（Phase 1 仅 toast 提示须先解锁后从设置导入，无法在锁定态完成恢复，易误导用户）；保留「我记得恢复密钥」与「我什么都没有」两条路径。JSON/CSV 备份导入仍在 **设置 → 数据 → 导入数据**（须已知备份时主密码并解锁）。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.21.0（开机自动启动、`SecuritySettings.launchAtLoginEnabled`、`launch_at_login_enabled` 设置键、恢复菜单精简等）。

## [1.20.0] - 2026-06-15

### 新增

- **主窗口置顶** — 标题栏 **图钉** 按钮（换肤 / 锁定与窗口控制之间）可切换 **窗口置顶**；主窗口与详情小窗口共用 `window:get-always-on-top` / `window:toggle-always-on-top` IPC（按 `event.sender` 对应窗口 `setAlwaysOnTop`）。

### 改进

- **PanelEdge 分割线** — `.panel-edge-sash` 默认显示静态分割线（`--border-default`）；动森皮肤常驻 **2px**（`--border-strong`），悬停/拖拽仍为青绿色粗线。
- **面板宽度拖拽** — 侧栏/详情 `PanelEdge` 使用 **Pointer Capture** 与 `pointermove` / `pointerup` / `pointercancel`，在输入框等区域松手可正确结束调宽。

### 修复

- **动森皮肤分割线** — v1.17.0 `PanelEdge` 重构后默认透明，三栏背景统一为 `bg-surface` 导致分割线不可见。
- **动森展开高亮** — 侧栏/详情从收起展开时偶发绿色高亮分割线（`collapsed` 变化时清除悬停并 `suppressHover` 250ms）。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.20.0（主窗口置顶、PanelEdge/动森 UI、拖拽修复等）。

## [1.19.0] - 2026-06-15

### 新增

- **文件夹同步（Folder Sync）** — 参考 Enpass 模式：选择本地或云盘同步目录，读写加密 `vault.pwdbook`；**设置 → 数据 → 同步** 先进入 **同步方式选择页**（Sync Hub），再选 **局域网同步** 或 **文件夹同步**。
- **Sync Hub** — `SyncHubView.vue`：两张方式卡片（Wi-Fi / 文件夹）；Wi-Fi 同步页返回按钮改为回到 Hub 而非设置页。
- **FolderSyncView** — 选文件夹、主密码确认连接、自动同步开关（变更 debounce 3s）、立即同步、更改目录、断开连接；合并冲突复用 `SyncConflictModal`。

### 改进

- **同步入口** — 设置 → 数据 → **同步** 打开 Sync Hub（`AppScreen = 'sync'`），不再直接进入 Wi-Fi 页。

### 文档

- 新增 [docs/code-map/folder-sync.md](./docs/code-map/folder-sync.md)；README、CHANGELOG 与 code-map 同步 v1.19.0。

## [1.18.0] - 2026-06-10

### 改进

- **分类切换** — 切换分类时 **不再自动选中第一条目**，右侧详情区清空，需手动点击列表项；分类条目 `pointerdown` 时 **立即切换分类**，与按住拖动排序配合更顺畅（`VaultSidebar.vue`、`useAppState.ts`）。
- **IPC 错误链** — 主进程 `handlers.ts` 的 `wrap` 与 Preload `invoke` 抛出错误时保留 `{ cause: error }`，DevTools 可追溯原始异常。

### 修复

- **启动崩溃** — v1.17.0 因 `handlers.ts` 缺少 `openLocalProgram` 导入导致应用无法启动（v1.17.1 热修，本版正式包含）。

### 开发

- **ESLint** — 新增 `eslint.config.mjs`（flat config）；`npm run lint` / `lint:fix`；全项目 Vue/TS 模板格式与可访问性规则对齐。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.18.0（分类切换、启动修复、IPC 错误链、ESLint 等）。

## [1.17.0] - 2026-06-10

### 新增

- **浏览器扩展安装向导** — **设置 → 安全** 新增 **「安装向导」** 按钮，打开分步引导弹窗（6 步：开启自动填充、安装扩展、复制扩展 ID、注册 Native Host、重启浏览器、解锁使用）；每步配有示意图（`BrowserExtensionGuideVisual`）；**打开扩展管理页** 时按本机已安装的 Chrome / Edge 选择 `chrome://extensions` 或 `edge://extensions` 地址并 **复制到剪贴板**（`browserLaunchService` + IPC `shell:open-extensions-page`）。

### 改进

- **面板边缘组件（PanelEdge）** — 侧栏与详情侧栏共用 `PanelEdge.vue`：4px 分割条 + 悬停/收起时显示的 **圆形描边箭头** 折叠钮（主题色边框，悬停实心背景）；分割线在按钮处挖空，按钮 `z-index` 置顶避免被邻列遮挡；`--panel-edge-width` 写入 `tokens.css`。
- **分类拖拽排序** — 移除左侧六点拖拽把手；在分类条目上 **按住并纵向拖动** 即可排序（搜索激活时禁用）；需纵向移动 **≥15px** 才进入拖拽，避免短按误触发；拖动时列表边缘自动滚动，收起横向溢出与缩放动画导致的卡顿。
- **暗黑模式** — 背景色阶整体上移（更浅），条目列表行使用 `--bg-elevated` 与背景区分。
- **动森皮肤** — 自定义指针缩至 16px；侧栏/详情外壳 `overflow: visible` 避免折叠钮被裁切；收起窄条背景与内容区一致；移除工具区上方黄色波浪分割线；标签筛选展开时 `overflow` 修正。

### 修复

- **浏览器自动填充** — 跳过隐藏/零尺寸的密码与用户名字段；通过 `HTMLInputElement` 原型 `value` setter 写入，兼容 React / Vue 等受控输入框。
- **面板折叠** — 经典模式下侧栏/详情收起窄条背景与列表区不一致；动森皮肤下折叠按钮显示不全。
- **分类拖拽** — 长列表向下拖动时出现横向滚动条、拖动不流畅；短按切换分类时条目位置偶发跳动。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.17.0（安装向导、PanelEdge、分类拖拽、暗黑/动森 UI、填充修复等）。

## [1.15.0] - 2026-06-09

### 改进

- **浏览器填充条 UI** — 登录页 **PwdBook** 填充条标题栏可 **拖拽** 移动位置（限制在视口内）；**收起** 按钮可折叠为仅图标 + 展开按钮；位置与收起状态写入页面 `localStorage`（`pwdbook-ui-x` / `pwdbook-ui-y` / `pwdbook-ui-collapsed`），跨同站点页面保持。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.15.0（浏览器填充条拖拽与收起等）。

## [1.14.0] - 2026-06-09

### 新增

- **详情小窗口** — 条目详情标题栏新增 **在新窗口打开** 按钮；独立无边框小窗口（`detail.html` → `DetailWindowApp`）展示 `PasswordDetail`；主窗口列表区自动占满原详情位；主/小窗口切换选中条目时双向同步；锁定保险库时自动关闭小窗口。
- **小窗口置顶** — 详情小窗口标题栏图钉按钮可切换 **窗口置顶**（`setAlwaysOnTop`）。

### 改进

- **左侧侧栏收缩** — 分类导航侧栏右缘可 **收起** 至 40px 窄条（`pwdbook-sidebar-collapsed` 写入 `localStorage`）；视口宽度不足时自动收起；展开/收起按钮位于面板边缘。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.14.0（侧栏收缩、详情小窗口、小窗口置顶等）。

## [1.13.0] - 2026-06-09

### 改进

- **TOTP 密钥显示/隐藏** — 条目详情 **两步验证密钥** 字段新增眼睛按钮，编辑时可切换明文/掩码显示（与密码字段一致）。
- **邮箱备份 SMTP 密码** — 已保存密码时显示 `********` 占位；聚焦后清空以便重新输入；支持显示/隐藏切换；移除冗余提示文案。
- **标签筛选** — 侧栏 **按标签筛选** 面板默认改为 **收起**（仍写入 `localStorage`）；搜索框高度与内边距在经典/动森皮肤下统一。
- **经典 UiInput 布局** — `class` / `style` 作用于外层包裹而非内部 `<input>`，修复详情侧栏等 `field-row` 中带 `class` 的输入框 flex 宽度异常。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.13.0（TOTP 显示切换、邮箱备份 SMTP 密码、标签筛选默认收起、UiInput 布局等）。

## [1.12.0] - 2026-06-08

### 新增

- **TOTP 两步验证** — 条目详情新增 **两步验证密钥（TOTP）** 字段；粘贴 Base32 密钥后实时显示 6 位动态验证码（30 秒刷新），可一键复制；密钥与会话密钥一同 AES 加密存储，并参与 Wi-Fi 同步与恢复密钥重加密。
- **密码健康** — 侧栏 **工具与设置** 新增 **密码健康** 入口；本地分析弱密码与重复密码，汇总统计并支持跳转对应条目。
- **侧栏标签筛选** — 分类列表下方新增可折叠 **按标签筛选** 面板：支持搜索标签（含拼音首字母）、**多选**（同时包含所选标签，AND 逻辑）、整行点击切换；展开状态写入 `localStorage`。
- **备注搜索** — 条目列表与快捷搜索条检索范围扩展至 **备注** 字段（仍支持拼音首字母）。
- **Wi-Fi 同步冲突 UI** — 客户端同步完成后，若存在两端时间戳相同的同时修改，弹出 **同步冲突** 对话框列出保留本地版本的条目；Toast 摘要含冲突条数。
- **托盘菜单国际化** — 系统托盘右键菜单（显示主窗口 / 快捷搜索 / 退出）随 **设置 → 外观 → 语言** 切换中英文。
- **macOS Native Host** — 浏览器自动填充注册支持 **macOS**：写入 `~/Library/Application Support/{Chrome,Edge,Chromium}/NativeMessagingHosts/`，入口脚本 `pwdbook-native-host.sh`。

### 改进

- **恢复密钥重加密** — 使用恢复密钥重置主密码时，同步重加密条目的 TOTP 密钥字段。
- **共享层** — TOTP、密码健康、恢复密钥格式校验、托盘文案等逻辑抽取至 `src/shared/` 并补充单元测试。

### 修复

- **动森皮肤** — `UiSelect` 下拉菜单被侧栏或其它层遮挡；下拉列表过长时无法滚动。
- **标签筛选** — 点击复选框区域外整行即可选中/取消标签。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.12.0（TOTP、密码健康、标签筛选、同步冲突 UI、macOS Native Host 等）。

## [1.11.0] - 2026-06-06

### 改进

- **彩色导航图标** — 侧栏工具区底部（分类管理、标签管理、回收站、设置、锁定）与 **设置** 页左侧 Tab 使用 pastel 彩色徽章（`IconBadge`），与分类列表图标风格一致。
- **图标选择器** — 可选图标从 30 个扩充至 **60 个图形图标** + **A–Z 共 26 个字母图标**；分为 **「图标」/「字母」** 两个 Tab，支持搜索过滤。
- **侧栏工具区布局** — **随机密码** 改为与分类管理等同款的列表行样式；**邮箱备份** 入口移至 **设置 → 安全**（「打开邮箱备份」按钮）。
- **邮箱备份导航** — 邮箱备份页返回改为回到 **设置 → 安全**（文案「返回设置」），不再回到主界面。

### 修复

- **动森皮肤** — 图标选择器搜索框外部放大镜与输入框 prefix 重叠导致偏移。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.11.0（UI 优化、图标与邮箱备份入口调整）。

## [1.10.0] - 2026-06-06

### 新增

- **macOS 打包** — `electron-builder` 增加 `mac` / `dmg` 配置，支持 **x64** 与 **arm64**；产物命名 `PwdBook-{version}-{arch}.dmg`。
- **npm 脚本** — `npm run dist:mac`（DMG 安装包）、`npm run dist:mac:dir`（未打包目录，便于本地调试）。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.10.0（macOS 构建说明）。

## [1.9.0] - 2026-06-06

### 新增

- **Wi-Fi 局域网同步** — **设置 → 数据 → 同步**；参考 Enpass 模式，桌面作 **服务端**、其他设备作 **客户端**，数据不经互联网。
- **服务端** — 本机 HTTPS WebDAV + mDNS 广播；显示局域网地址、访问密码、**校验码**、配对 **二维码** 与 JSON；支持已配对设备列表；保险库变更后自动重建加密同步包。
- **客户端（桌面）** — mDNS 发现局域网服务、校验码对照、输入访问密码后 **立即同步**；支持手动粘贴配对 JSON。
- **同步内核** — 加密 `SyncBundle`（`.pwdbook-sync` 格式）、按条目 `id` + `updated_at` **LWW 合并**、回收站 `deleted_at` 参与冲突；共享层 `mobileSyncWorkflow` 供未来移动端复用。

### 改进

- 同步页侧栏 **「我是服务端 / 我是客户端」**；主内容区按角色展示专属教程、示意图高亮与操作面板。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.9.0；新增 [docs/code-map/wifi-sync.md](./docs/code-map/wifi-sync.md)。

## [1.8.0] - 2026-06-05

### 新增

- **自动锁定：120 分钟** — **设置 → 安全** 自动锁定下拉框新增 120 分钟选项；沿用现有空闲计时逻辑。
- **自动锁定：跟随系统锁屏** — 新增「跟随系统锁屏」选项；系统锁屏时主进程通过 `powerMonitor` 清除会话密钥并通知渲染进程回到锁定页（`auto_lock_minutes = -1`）。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.8.0（自动锁定 120 分钟、跟随系统锁屏）。

## [1.7.0] - 2026-06-05

### 新增

- **标题栏快捷锁定** — 换肤按钮右侧新增锁定按钮，一键锁定保险库。
- **回收站** — 删除条目改为**移至回收站**（软删除）；侧栏底部入口，展示条数角标；支持单条/全部还原、单条/清空彻底删除；显示删除时间与剩余保留天数。
- **回收站保留期限** — **设置 → 安全** 可配置 7 / 14 / 30 / 60 / 90 天（默认 30 天）；过期条目在应用启动与相关操作时自动清理。
- **拼音首字母搜索** — 条目列表与快捷搜索条支持按标题、用户名、网址、分类名、标签的**拼音首字母**检索（如输入 `cs` 匹配「测试」）；列表搜索命中时高亮原文子串。

### 改进

- **默认排序** — 条目列表默认排序改为 **标题 A–Z**（原为「最近使用」）。
- **删除确认弹窗** — 列表/右键删除改用应用内 `UiModal` 确认（替代 Electron 原生 `window.confirm`），点击删除后菜单立即关闭；动森皮肤缩小确认文案、统一按钮风格；经典模式加大操作按钮。

### 移除

- **打开网址携带账号密码** — 移除设置项与相关逻辑；列表菜单与快捷搜索条打开网址时仅打开原始链接，不再附加 `user` / `pwd` 查询参数。

### 修复

- 动森皮肤删除确认弹窗在自定义页脚时因 `show-footer=false` 导致操作按钮不显示。

### 文档

- README、CHANGELOG 与 code-map 同步 v1.7.0（回收站、拼音搜索、删除弹窗、默认排序等）。

## [1.6.0] - 2026-06-06

### 新增

- **浏览器自动填充（Chrome / Edge）** — **设置 → 安全** 可开启本机桥接；`extension/` MV3 扩展在登录页按域名匹配条目并填充；Native Host（`native-host/`）+ 主进程 `127.0.0.1` 桥接；无业务出站。
- **设置页一键注册扩展** — 填写 Chrome 扩展 ID 后点击「注册到 Chrome / Edge」，写入用户目录清单与系统注册表；可打开 `chrome://extensions/`；仍保留 `npm run register-native-host`。

### 改进

- 扩展填充条 UI 对齐经典主题（黄铜强调色）；多账号自定义下拉，避免与页面 MutationObserver 冲突。
- 桥接 TCP 断连（`ECONNRESET`）不再导致主进程崩溃；Native Host 正确读取 `%APPDATA%\pwd-book\native-bridge.json`。

### 文档

- 新增 [docs/code-map/browser-autofill.md](./docs/code-map/browser-autofill.md)；更新 code-map 索引、IPC、数据库与主进程说明；README 浏览器集成章节。

## [1.5.0] - 2026-06-05

### 新增

- **PwdBook CSV 导入** — **设置 → 数据 → 导入数据** 新增 PwdBook CSV 来源；可导入本应用「导出数据 → CSV 表格」生成的文件，按「分类」列还原分类，行为与 JSON 备份一致。

### 文档

- README、code-map 同步说明 CSV 可导入；版本号更新为 v1.5.0。

## [1.4.0] - 2026-06-04

### 新增

- **标题栏换肤** — 标题栏 **调色板** 按钮打开皮肤菜单，快速切换 **经典保险库** / **Animal Island**；可跳转 **设置 → 外观** 调整深浅色与强调色。

### 改进

- **导出数据向导** — **设置 → 数据** 合并为单一 **导出数据** 入口；两步向导内分 **PwdBook 备份**（JSON / CSV / Excel）与 **迁移到其他应用**（KeePass、Enpass、Bitwarden、1Password、Chrome CSV）两组卡片选择。
- **导出确认页** — PwdBook 格式展示将全部导出的条数；第三方 CSV 展示 **将导出** / **因缺少标题或密码将跳过** 统计、期望列名及各目标应用的导入指引。
- **导出 UI** — 卡片式格式选择与明文密码安全提示；JSON 备份单独标注可重新导入。

## [1.3.0] - 2026-06-03

### 新增

- **分类右键菜单** — 侧栏自定义分类右键：**编辑**（打开分类管理编辑对话框）、**删除**（仅空分类可删；分类下有条目时禁用并提示）。
- **创建副本** — 条目右键或「⋯」菜单新增 **创建副本**，复制当前条目全部字段，标题追加 ` - 副本`（英文界面为 ` - Copy`）。

### 改进

- **列表菜单文案** — 「复制数据」改为 **复制到剪贴板**，与 **创建副本** 区分歧义。

### 修复

- 条目右键时未高亮选中当前条目。
- 分类右键菜单「编辑」显示未翻译的 `common.edit` 键（补充 `common.edit` 国际化）。

## [1.2.1] - 2026-06-02

### 新增

- **快捷键唤起主窗口** — 全局快捷键（默认 `Alt+Shift+M`），可在 **设置 → 安全** 启用/禁用。
- **快捷条「最近打开」** — 无搜索词时展示最近打开的 5 个条目；悬停可手动移除；再次打开条目后补回列表。

### 改进

- **快捷条选中高亮** — 键盘/鼠标选中条目时对比度增强。
- **列表右键菜单** — 「移动到」子菜单视口自适应定位；分类最多展示 5 条并支持滚动；弹出卡片背景与列表区分。
- **条目标签位置** — 标签紧贴标题右侧（5px 间距）。

### 修复

- 快捷条清空「最近打开」后不应再从历史 `last_used_at` 自动补满 5 条（独立 `quick_bar_recent_ids` 存储）。
- 「移动到」子菜单在底部/移入时定位跳动。
- 右键菜单与子菜单在窗口边缘被裁切。

### 文档

- 新增 [docs/code-map/quickbar-and-shortcuts.md](./docs/code-map/quickbar-and-shortcuts.md)（快捷条、最近打开、全局快捷键与调试说明）。

## [1.2.0] - 2026-06-02

### 新增

- **悬浮快捷搜索条** — 屏幕顶部置顶的细长搜索窗口，快速检索条目；默认快捷键 `Alt+Shift+P`（可在 **设置 → 安全** 开关）。`Enter` 或点击结果将**直接**启动条目配置的本地程序，或按设置打开网址（不再跳转主界面）。托盘菜单提供「快捷搜索」入口；保险库锁定后唤起快捷条会引导打开主窗口解锁。
- **从其他应用导入（CSV / JSON）** — **设置 → 数据 → 从其他应用导入**，三步向导：选择来源 → 上传文件 → 预览确认。支持 **KeePass、Enpass、Bitwarden、1Password、Chrome 密码 CSV** 及 **PwdBook JSON 备份**；自动按来源归入对应分类，预览区区分「将导入 / 重复跳过 / 无效」并展示原因。
- **导出到其他应用（CSV）** — **设置 → 数据 → 导出到其他应用 (CSV)**，按目标应用格式导出（KeePass、Enpass、Bitwarden、1Password、Chrome），便于迁移或双库并存。
- **本地程序路径** — 条目详情新增「本地程序路径」字段；列表右键菜单可「打开程序」；快捷搜索条 `Enter` 时若已填写路径则优先启动该程序。
- **打开网址是否携带账号密码（可配置）** — **设置 → 安全** 新增开关。开启后，列表菜单「打开网址」会在 URL 上附加 `user`、`pwd` 查询参数；关闭则仅打开原始网址。菜单文案随开关变化。
- **条目详情默认只读** — 打开已有条目时先以只读展示，点击「编辑」后再修改，减少误触改动。
- **侧栏新建分类快捷入口** — 分类搜索框旁增加「+」按钮，无需进入分类管理即可新建分类。
- **搜索框一键清除** — 条目列表搜索、侧栏分类搜索等 `UiInput` 支持清除按钮（`allow-clear`）。

### 改进

- **导入流程** — 向导式 UI、重复检测与无效条目分类展示；修复通过 IPC 传递数据时 `An object could not be cloned` 导致导入失败的问题。
- **导出** — JSON / Excel 行为保持；新增多格式 CSV 导出对话框与列映射。
- **侧栏工具区** — 「工具与设置」区块默认**收起**，展开状态写入 `localStorage`（`pwdbook-sidebar-utilities-expanded`）。
- **界面交互** — 多项列表、详情、菜单与布局微调（含右键「移动到」子菜单定位、长文本溢出等）。
- **Animal Island 皮肤** — 自定义鼠标指针尺寸调整。

### 修复

- 导入克隆错误、部分 UI 交互回归问题。
- 动森主题指针显示过大。

---

## [1.1.1] - 2026-05-30

维护版本，无新功能；聚焦详情侧栏、列表菜单与主题样式修复。同期 README 补充了自 v1.1.0 起已上线能力的说明（标签、排序、侧栏行为等）。

### 修复

- **详情侧栏布局** — 修正右侧详情区 UI（宽度分配、滚动与间距），避免与列表区争抢空间。
- **长文本溢出** — 详情表单字段过长时不再把布局挤出可视区域；`VaultView` 三栏布局增加 `min-width: 0` 等约束。
- **右键菜单定位** — 条目列表右键菜单及「移动到其他分类」子菜单改为按视口边界计算位置，避免贴边或超出屏幕。
- **滚动条样式** — 统一经典主题与 Animal Island 皮肤下滚动条外观（`global.css` / `tokens.css` / `animal-skin.css`）。

---

## [1.1.0] - 2026-05-30

### 新增

- **标签管理** — 侧栏「标签管理」：新建、重命名、删除标签（同步更新所有关联条目）；详情页从已有标签选用，支持搜索与条目数统计。
- **列表排序** — 条目列表支持 **最近使用 / 标题 A–Z / 创建时间** 三种排序。
- **详情侧栏交互** — 选中条目或「新建条目」时自动展开侧栏；新建时可一键生成密码；取消创建会收起侧栏。
- **列表菜单** — 右键或「⋯」：**移动到其他分类**、**在浏览器中打开网址**（URL 附加 `user` / `pwd` 参数）。
- **开发工具** — `npm run screenshots:animal` 快速生成 Animal Island 主题 README 截图。

### 改进

- **JSON 导入** — 导入备份时自动创建备份中缺失的分类。
- **Animal Island 皮肤** — 动森风格 UI 统一与多项细节修复；经典模式输入框尺寸调整。

### 修复

- 邮箱备份页白屏、标签 CRUD 相关问题、侧栏展开/收起逻辑、动森 UI 回归问题等。

---

## 更早版本

1.0.0 及更早功能（Animal Island 皮肤、邮箱备份、随机密码、Excel 导出等）见 [README.md](./README.md) 功能章节与 git 标签 `release-v1.1.0` 之前的提交历史。
